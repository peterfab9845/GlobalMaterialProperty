# Global Material Property
Adds a component which generates a menu item controlling one or more material
properties on all renderers.

## Usage
1. Add the Global Material Property component to a new GameObject
2. Configure the parameter name, default, animation curve, and material property name(s)
3. Add an MA Menu Installer component to the same GameObject
